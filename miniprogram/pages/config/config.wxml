<view class="container">
 <view wx:if="{{!isRun}}">
  请连接到wifi XPG-GAgent-***
 </view>
 <view wx:if="{{isRun && devices.length === 0}}">
  正在发现设备
 </view>
 <view wx:for="{{devices}}">
  {{index}}: {{item.did}}
  </view>
</view>
